{
  "name": "weather-proxy",
  "version": "1.1.0",
  "description": "Pulls weather data from Dark Sky and simplifies it down to a tiny bit of JSON/CSV",
  "main": "build/index.js",
  "scripts": {
    "start": "node build/index.js",
    "build": "babel src/ -d build/",
    "dev": "babel -w src/ -d build/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Drew Lustro",
  "license": "MIT",
  "repository": "https://github.com/drewlustro/weather-proxy",
  "dependencies": {
    "chalk": "^1.1.3",
    "request": "^2.79.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-0": "^6.16.0",
    "eslint": "^3.12.0",
    "eslint-config-airbnb": "^13.0.0"
  }
}
